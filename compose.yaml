version: '3'

services:
  back-end:
    container_name: elit-fasad-frontend
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    ports:
      - '4200:4200'
    volumes:
      - .:/app
      - /app/node_modules
